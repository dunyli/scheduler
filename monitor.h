#ifndef MONITOR_H
#define MONITOR_H

/**
 * ============================================
 * МОДУЛЬ МОНИТОРИНГА СИСТЕМЫ
 * ============================================
 *
 * Назначение: Получение и отображение информации о реальной системе
 * - Использование памяти
 * - Загрузка CPU
 * - Список запущенных процессов (до 50)
 *
 * Управление:
 * - '0' - выход из монитора
 * - Любая другая клавиша - обновить информацию
 *
 * Это делает программу аналогом top/htop для Windows
 */

#ifdef __cplusplus
extern "C" {
#endif

	/**
	 * Показывает загрузку CPU и использование памяти
	 *
	 * Выводит в консоль:
	 * - Процент использования памяти
	 * - Использовано MB / Всего MB
	 * - Процент загрузки CPU
	 */
	void show_cpu_memory(void);

	/**
	 * Показывает список реальных процессов в системе
	 *
	 * Выводит таблицу с:
	 * - PID (идентификатор процесса)
	 * - Имя процесса
	 * - Использование памяти в KB
	 * - Приоритет выполнения
	 *
	 * Показывает до 50 процессов (отсортированных по памяти)
	 */
	void show_real_processes(void);

	/**
	 * Главный режим мониторинга системы
	 *
	 * Запускает интерактивный монитор:
	 * 1. Показывает информацию о системе
	 * 2. Ожидает нажатия клавиши для обновления
	 * 3. Выход по нажатию '0'
	 *
	 * Управление:
	 * - Любая клавиша (кроме 0) - обновить информацию
	 * - '0' - выход
	 */
	void system_monitor_mode(void);

#ifdef __cplusplus
}
#endif

#endif /* MONITOR_H */